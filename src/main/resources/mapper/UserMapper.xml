<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personrecipe.mapper.UserMapper">

    <sql id="baseColumns">
        id, openid, unionid, nickname, avatar_url, session_key, last_login_at, created_at, updated_at
    </sql>

    <select id="findByOpenid" resultType="com.personrecipe.entity.User">
        SELECT
        <include refid="baseColumns"/>
        FROM users
        WHERE openid = #{openid}
    </select>

    <select id="findById" resultType="com.personrecipe.entity.User">
        SELECT
        <include refid="baseColumns"/>
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (openid, unionid, nickname, avatar_url, session_key, last_login_at)
        VALUES (#{openid}, #{unionid}, #{nickname}, #{avatarUrl}, #{sessionKey}, #{lastLoginAt})
    </insert>

    <update id="update">
        UPDATE users
        <set>
            <if test="unionid != null">unionid = #{unionid},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="sessionKey != null">session_key = #{sessionKey},</if>
            <if test="lastLoginAt != null">last_login_at = #{lastLoginAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="clearSessionKey">
        UPDATE users
        SET session_key = NULL
        WHERE id = #{id}
    </update>

</mapper>
