<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personrecipe.mapper.RecipeStepMapper">

    <insert id="insertBatch">
        INSERT INTO recipe_steps (recipe_id, step_number, description, image_url, sort_order)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.recipeId}, #{item.stepNumber}, #{item.description}, #{item.imageUrl}, #{item.sortOrder})
        </foreach>
    </insert>

    <select id="findByRecipeId" resultType="com.personrecipe.entity.RecipeStep">
        SELECT id, recipe_id, step_number, description, image_url, sort_order, created_at, updated_at
        FROM recipe_steps
        WHERE recipe_id = #{recipeId}
        ORDER BY sort_order ASC, step_number ASC, id ASC
    </select>

    <delete id="deleteByRecipeId">
        DELETE FROM recipe_steps
        WHERE recipe_id = #{recipeId}
    </delete>

</mapper>
